<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.mssu.cis385.implicitintentsreceiver.chaoscontrol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_chaos_launch"
        android:roundIcon="@mipmap/ic_chaos_launch_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChaosControl">
        <activity
            android:name=".ShoppingList"
            android:exported="true"
            android:label="Shopping List"
            android:parentActivityName=".MainActivity"/>
                <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="edu.mssu.cis385.implicitintentsreceiver.chaoscontrol.MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>